<div class="container">
  <h4>コメントの検索内容</h4>
  <% if @model == 'user' %>
    <div class="d-flex">
      <% unless @keyword == '' %>
        <h5>検索名：<%= @keyword %>（ユーザー）</h5>
      <% else %>
        <h5>検索名：全ユーザー</h5>
      <% end %>
      <p>（マッチした検索数<%= @comments.count %>件）</p>
    </div>
    <% if @comments.present? %>
      <%= render 'list', comments: @comments %>
    <% else %>
      検索したユーザーのコメントは見つかりませんでした。
    <% end %>
  <% elsif @model == 'post' %>
    <div class="d-flex">
      <% unless @keyword == '' %>
        <h5>検索名：<%= @keyword %>（投稿）</h5>
      <% else %>
        <h5>検索名：全投稿</h5>
      <% end %>
      <p>（マッチした検索数<%= @comments.count %>件）</p>
    </div>
    <% if @comments.present? %>
      <%= render 'list', comments: @comments %>
    <% else %>
      検索した投稿は見つかりませんでした。
    <% end %>
  <% end %>
  <div class="row justify-content-center pt-3">
    <%= link_to 'コメント一覧に戻る', admin_comments_path, class:'btn btn-outline-primary' %>
  </div>
</div>
