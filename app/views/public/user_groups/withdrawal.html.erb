<div class="container">
  <div class="row">
    <% if @leader %>
      <h5>
        あなたは<span class="text-primary"><%= @group.name %></span>のグループリーダーです。<br>
        グループチャットのリーダーが退会する場合は<br>
        グループチャットが削除されます。
      </h5>
    <% elsif @ejected %>
      <h5><span class="text-primary"><%= @with_member.user.name %></span>さんをグループから退会させますか？</h5>
    <% else %>
      <h5>"<%= @group.name %>"のグループを退会しますか？</h5>
    <% end %>
  </div>
  <div class="row">
    <% if @ejected %>
      <%= link_to '退会させる', room_group_user_group_path(@group.id, @with_member.id, removed?: true), method: :delete, class:"btn btn-danger"%>
    <% else %>
      <%= link_to 'グループチャットを退会する', room_group_user_group_path(@group.id, @current_user_group.id), method: :delete, class:"btn btn-danger"%>
    <% end %>
    <%= link_to 'グループチャットに戻る', room_group_path(@group.id), class:"btn btn-success"%>

  </div>
</div>