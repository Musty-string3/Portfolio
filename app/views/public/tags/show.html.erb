<div class="container">
  <div class="text-center">
    <h4>
      "<%= @tag.name %>"タグの投稿一覧
      <span class="h6">
        (<%= @posts.count %>件)
      </span>
    </h4>
  </div>
  <%= render 'public/posts/index', posts: @posts %>
</div>
