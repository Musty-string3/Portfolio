<!--フォロワー一覧画面-->
<div class="container">
  <h3 class="text-center pt-3 pb-3">
    <%= @user == current_user ? 'フォロー中' : "#{@user.name}さんのフォローしているユーザー" %>
  </h3>
  <%= render 'follow_list', users: @users %>
</div>