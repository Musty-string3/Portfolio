<div class="container">
  <h4 class="text-center pb-3">あなたがいいねした投稿一覧</h4>
  <% if @posts.present? %>
    <%= render 'public/posts/index', posts: @posts, tag_counts: @tags %>
  <% else %>
    <h5>
      いいねした投稿はまだありません。
    </h5>
  <% end %>
</div>