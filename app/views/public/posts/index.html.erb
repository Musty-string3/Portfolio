<!--投稿一覧  -->
<div class="container">
  <% if @posts.present? %>
  <div class="row">
    <div class="col-12 ml-3">
      <h4 class="text-center"><%= @sort_condition %>で表示しています</h4>
    </div>
    <div class="col-lg-12 text-right">
      <%= form_with url: posts_path, method: :get do |f| %>
        <%= f.select :sort, options_for_select({"新着投稿"=>"new", "いいね順"=>"like", "直近のいいね順"=>"likes_in_week" }) %>
        <%= f.submit "検索する" %>
      <% end %>
    </div>
  </div>
    <%= render 'index', posts: @posts %>
  <% else %>
    <h5>
      投稿はまだありません。<br>
      ユーザーの皆さんに自身の投稿を見てもらえるよう<br>
      画面上部の「新規投稿」から投稿してみよう！！
    </h5>
  <% end %>
</div>
