<div class="container">
  <% if @User == current_user %>
    <h4>マイページ</h4>
    <% if flash[:notice] %>
      <%= flash[:notice] %>
    <% end %>
    <div class="row">
      <div class="col-lg-2">
        <%= image_tag @user.get_profile_image(100, 100) %>
      </div>
      <div class="col-lg-5">
        <%= @user.name %><br>
        投稿数<%=%>
      </div>
    </div>
    <div class="row">
      <p>自己紹介：　<%= @user.introduction %></p>
    </div>
    <div class="row">
      <%= link_to "プロフィールを編集", edit_user_path(@user), class:"col-lg-2" %>
      <%= link_to "いいねした一覧", likes_user_path(@user), class:"col-lg-2" %>
      <%= link_to "投稿した一覧", index_current_posts_path, class:"col-lg-2" %>
    </div>
  <% else %>
    <h4><%= @User.name %>さんのマイページ</h4>
    <% if flash[:notice] %>
      <%= flash[:notice] %>
    <% end %>
    <div class="row">
      <div class="col-lg-2">
        <%= image_tag @User.get_profile_image(100, 100) %>
      </div>
      <div class="col-lg-5">
        <%= @User.name %><br>
        投稿数<%=%>
      </div>
    </div>
    <div class="row">
      <p>自己紹介：　<%= @User.introduction %></p>
    </div>
    <div class="row">
    <p>ここに各ユーザーが投稿した内容をつける</p>
    </div>
  <% end %>
</div>
